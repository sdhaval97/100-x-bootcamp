// warming up variables

let number = 3;
number = 7;

// const can only be defined once
function calculateArithmetic(a, b, type) {
    if (type == "sum") {
        return a+b;
    }
    if (type == "minus") {
        return 
    }
}